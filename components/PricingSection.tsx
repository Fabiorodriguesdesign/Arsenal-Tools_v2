
import React from 'react';

// This component is obsolete and has been replaced by the LeadCaptureModal flow.
const PricingSection: React.FC = () => null;

export default React.memo(PricingSection);
